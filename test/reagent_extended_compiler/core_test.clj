(ns reagent-extended-compiler.core-test
  (:require [clojure.test :refer :all]
            [reagent-extended-compiler.core :refer :all]))

(deftest a-test
  (testing "FIXME, I fail."
    (is (= 0 1))))
